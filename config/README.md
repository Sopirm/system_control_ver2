# Конфигурация окружений

Этот документ описывает все доступные переменные окружения для системы управления задачами.

## Структура конфигурации

```
config/
├── environments/           # Файлы конфигурации для разных окружений
│   ├── development.env     # Development окружение
│   ├── test.env           # Test окружение
│   └── production.env     # Production окружение
├── env_loader.go          # Утилита для загрузки конфигурации
├── go.mod                 # Go модуль для конфигурации
└── README.md             # Этот файл
```

## Переменные окружения

### 🌍 Общие переменные

| Переменная | Описание | Development | Test | Production |
|------------|----------|-------------|------|------------|
| `ENVIRONMENT` | Текущее окружение | `development` | `test` | `production` |

### 🚪 API Gateway

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|-------------|
| `API_GATEWAY_PORT` | Порт API Gateway | Нет | `8080` |
| `JWT_SECRET` | Секретный ключ для JWT | **Да** | - |
| `RATE_LIMIT_RPS` | Лимит запросов в секунду | Нет | `10` (dev), `5` (prod) |
| `RATE_LIMIT_BURST` | Максимальный burst запросов | Нет | `20` (dev), `10` (prod) |

### 🗄️ База данных

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|-------------|
| `DB_HOST` | Хост базы данных | **Да** | `localhost` |
| `DB_PORT` | Порт базы данных | Нет | `5432` |
| `DB_NAME` | Имя базы данных | **Да** | `system_control_{env}` |
| `DB_USER` | Пользователь БД | **Да** | `postgres` |
| `DB_PASSWORD` | Пароль БД | **Да** | - |
| `DB_MAX_OPEN_CONNS` | Макс. открытых соединений | Нет | `10` (dev), `100` (prod) |
| `DB_MAX_IDLE_CONNS` | Макс. idle соединений | Нет | `5` (dev), `50` (prod) |
| `DB_CONN_MAX_LIFETIME` | Время жизни соединения | Нет | `300s` (dev), `1800s` (prod) |

### 👥 Service Users

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|-------------|
| `USERS_SERVICE_PORT` | Порт сервиса пользователей | Нет | `8081` |
| `USERS_SERVICE_URL` | URL сервиса пользователей | Нет | `http://localhost:8081` |

### 📦 Service Orders

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|-------------|
| `ORDERS_SERVICE_PORT` | Порт сервиса заказов | Нет | `8082` |
| `ORDERS_SERVICE_URL` | URL сервиса заказов | Нет | `http://localhost:8082` |

### 📝 Логирование

| Переменная | Описание | Development | Test | Production |
|------------|----------|-------------|------|------------|
| `LOG_LEVEL` | Уровень логирования | `debug` | `info` | `info` |
| `LOG_FORMAT` | Формат логов | `console` | `json` | `json` |
| `LOG_OUTPUT` | Вывод логов | `stdout` | `stdout` | `file` |
| `LOG_FILE_PATH` | Путь к файлу логов | - | - | `/var/log/system_control/app.log` |
| `LOG_ROTATION` | Ротация логов | - | - | `daily` |
| `LOG_MAX_SIZE` | Макс. размер лога | - | - | `100MB` |
| `LOG_MAX_FILES` | Количество файлов | - | - | `30` |

### 📨 События и брокер сообщений

| Переменная | Описание | Development | Test | Production |
|------------|----------|-------------|------|------------|
| `EVENTS_PUBLISHER` | Тип publisher событий | `inmemory` | `inmemory` | `kafka` |
| `EVENTS_BUFFER_SIZE` | Размер буфера событий | `100` | `1000` | `10000` |
| `KAFKA_BROKERS` | Адреса Kafka брокеров | - | - | **Обязательно** |
| `KAFKA_TOPIC` | Топик для событий | - | - | `system_control_events` |

### 🔒 Безопасность

| Переменная | Описание | Development | Test | Production |
|------------|----------|-------------|------|------------|
| `BCRYPT_COST` | Стоимость bcrypt | `10` | `8` | `14` |
| `SESSION_TIMEOUT` | Время жизни сессии | `24h` | `2h` | `8h` |
| `ENABLE_RATE_LIMITING` | Включить rate limiting | `true` | `true` | `true` |
| `ENABLE_IP_WHITELIST` | Включить IP whitelist | `false` | `false` | `true` |
| `ALLOWED_IPS` | Разрешенные IP адреса | - | - | **Обязательно для prod** |

### 🌐 CORS

| Переменная | Описание | Development | Test | Production |
|------------|----------|-------------|------|------------|
| `CORS_ALLOW_ALL_ORIGINS` | Разрешить все домены | `true` | `true` | `false` |
| `CORS_ALLOWED_ORIGINS` | Разрешенные домены | - | - | **Обязательно** |

### 🔐 TLS/SSL

| Переменная | Описание | Production |
|------------|----------|------------|
| `TLS_ENABLED` | Включить TLS | `true` |
| `TLS_CERT_FILE` | Путь к сертификату | **Обязательно** |
| `TLS_KEY_FILE` | Путь к приватному ключу | **Обязательно** |

### 📊 Мониторинг и метрики

| Переменная | Описание | Production |
|------------|----------|------------|
| `ENABLE_METRICS` | Включить метрики | `true` |
| `METRICS_PORT` | Порт для метрик | `9090` |
| `ENABLE_HEALTH_CHECKS` | Включить health checks | `true` |
| `HEALTH_CHECK_INTERVAL` | Интервал health checks | `30s` |
| `ENABLE_PROFILING` | Включить профилирование | `false` |

### 🗃️ Redis (только Production)

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `REDIS_HOST` | Хост Redis | **Да** |
| `REDIS_PORT` | Порт Redis | Нет (по умолчанию 6379) |
| `REDIS_PASSWORD` | Пароль Redis | **Да** |
| `CACHE_TTL` | TTL кеша | Нет (по умолчанию 3600s) |

### 🧪 Тестирование (только Test)

| Переменная | Описание | По умолчанию |
|------------|----------|-------------|
| `TEST_DATA_CLEANUP` | Очистка тестовых данных | `true` |
| `MOCK_EXTERNAL_SERVICES` | Мокать внешние сервисы | `true` |
| `PARALLEL_TEST_EXECUTION` | Параллельное выполнение тестов | `true` |
| `ENABLE_TEST_ROUTES` | Включить тестовые маршруты | `true` |

## Использование

### Загрузка конфигурации в Go

```go
package main

import (
    "config"
    "log"
)

func main() {
    // Создаем загрузчик конфигурации
    envLoader := config.NewEnvLoader("development", "./config/environments")
    
    // Загружаем переменные окружения
    if err := envLoader.LoadEnvironment(); err != nil {
        log.Fatalf("Ошибка загрузки конфигурации: %v", err)
    }
    
    // Проверяем обязательные переменные
    requiredVars := []string{"DB_HOST", "DB_NAME", "JWT_SECRET"}
    if err := envLoader.ValidateRequiredVars(requiredVars); err != nil {
        log.Fatalf("Отсутствуют обязательные переменные: %v", err)
    }
}
```

### Переопределение переменных

Переменные можно переопределить через:

1. **Системные переменные окружения** (высший приоритет)
2. **Файлы .env** 
3. **Значения по умолчанию** (низший приоритет)

### Секреты в Production

Для production используйте системы управления секретами:

```bash
# Через переменные окружения
export JWT_SECRET="$(vault kv get -field=jwt_secret secret/app)"
export DB_PASSWORD="$(vault kv get -field=db_password secret/app)"

# Или через Docker secrets
docker secret create jwt_secret /path/to/jwt_secret.txt
```

### Проверка конфигурации

```bash
# Проверить загруженную конфигурацию
make info

# Проверить переменные для development
source config/environments/development.env
env | grep -E "(DB_|JWT_|LOG_)" | sort
```

## Troubleshooting

### Частые ошибки

1. **Отсутствует переменная окружения**
   ```
   Ошибка: отсутствуют обязательные переменные окружения: JWT_SECRET
   Решение: Установите переменную или проверьте файл конфигурации
   ```

2. **Неверный формат конфигурации**
   ```
   Ошибка: некорректный формат в development.env:15: INVALID_LINE
   Решение: Проверьте синтаксис KEY=VALUE в указанной строке
   ```

3. **Файл конфигурации не найден**
   ```
   Ошибка: файл конфигурации не найден: config/environments/custom.env
   Решение: Создайте файл или используйте существующее окружение
   ```

### Отладка

Включите отладочный вывод переменных:

```go
envLoader.PrintLoadedVars([]string{
    "ENVIRONMENT", "DB_HOST", "JWT_SECRET", "LOG_LEVEL",
})
```

**Внимание:** Секретные переменные автоматически маскируются в выводе.
